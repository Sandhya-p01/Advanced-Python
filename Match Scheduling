import random
a=int(input("enter num of teams: "))
teams=[]
print("enter all team names--- ")
for i in range(a):
    t=input("enter team name: ")
    teams.append(t)
m=int(input("enter no.of matches met: "))
matches=[]
for i in range(0,a-1):
    for j in range(i+1,a):
        for k in range(m):
            matches.append([teams[i],teams[j]])
random.shuffle(matches)
p=1
for i in matches:
    print("Match {}: {} vs {}".format(p,i[0],i[1]))
    p+=1
